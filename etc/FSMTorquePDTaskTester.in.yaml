---
# If true, the FSM transitions are managed by an external tool
Managed: false
# If true and the FSM is self-managed, transitions should be triggered
StepByStep: true
# Change idle behaviour, if true the state is kept until transition,
# otherwise the FSM holds the last state until transition
IdleKeepState: false
# Where to look for state libraries
StatesLibraries:
- "@MC_STATES_DEFAULT_RUNTIME_INSTALL_PREFIX@"
- "@MC_STATES_RUNTIME_INSTALL_PREFIX@"
# Where to look for state files
StatesFiles:
- "@MC_STATES_DEFAULT_RUNTIME_INSTALL_PREFIX@/data"
- "@MC_STATES_RUNTIME_INSTALL_PREFIX@/data"
# If true, state factory will be more verbose
VerboseStateFactory: false
# Additional robots to load
robots:
  ground:
    module: env/ground
# General constraints, always on
# Collision constraint
collisions:
- type: collision
  useMinimal: true

ObserverPipelines:
- name: Pipeline
  observers:
    - type: Encoder
      config:
        velocity: encoderVelocities
    # - type: BodySensor
    #   update: false
    #   # config:
    #   #   bodySensor: FloatingBase

    - type: Tilt
      required: true
      update: true
      config:
        anchorFrameFunction: anchorFrameFunction
        filterGains:
          initAlpha: 2 # 5 # 0.0
          initBeta: 2 # 1 # 0.0 # 0.00001
          initGamma: 1 # 2 # 0.0
          finalAlpha: 2 # 5 # 0.0
          finalBeta: 2 # 1 # 0.0 # 0.00001
          finalGamma: 1 # 2 # 0.0

# Implement some additional text states
states: {}
# Transitions map
transitions:
- [FSMTorquePDTaskTester_Initial, OK, FSMTorquePDTaskTester_Initial, Strict]
# Initial state
init: FSMTorquePDTaskTester_Initial

left_hand_frame_name: left_hand_link
right_hand_frame_name: right_hand_link
relative_frame_name: pelvis
